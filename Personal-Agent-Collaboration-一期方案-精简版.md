# Personal Agent Collaboration 一期方案（精简版）

> 让我的 Agent 在获得你授权后，向你的项目 Agent 提问；由你确认答案后，再回复给我。

**架构方向：云端统一管理项目映射、审批和调度，本地只保留轻量 Connector，负责受控地启动项目 Agent。**

## 1. 要解决什么问题

团队里很多信息同步仍依赖人：找负责人、等对方查代码和文档、整理答案、回复。即使对方的 Agent 已经掌握项目上下文，这些工作也需要人手动完成。

我们希望把“查资料、整理回答”交给 Agent，把“是否允许访问、哪些内容可以分享”的决定留给人。

一期面向 **10 人以内的内部团队**，优先验证项目背景、技术方案、历史决策和项目状态查询是否能因此更高效，而不是建设完整的 Agent 社交网络。

## 2. 用户怎么用

以 Alex 向 Bob 询问推荐项目为例：

首次使用时，Bob 在本地 Connector 登录，选择允许协作的项目，将项目标识、设备、路径及 Runtime 同步到云端。只注册定位信息，不因此上传项目代码和文档。

1. **Alex 提问**：在自己的 Codex 中说：“帮我问 Bob，recommendation 项目为什么从 DIN 换成 Transformer？”
2. **平台转交**：记录请求并通知 Bob；Bob 电脑离线时，先保存请求，等上线后继续。
3. **Bob 批准执行**：在云端审批页面查看提问人、目标项目和问题，选择同意或拒绝。
4. **云端调度、本地执行**：云端找到 Bob 对应的设备和项目，将已批准任务交给 Connector；本地校验后进入项目目录，启动 Agent 生成答案。
5. **Bob 确认发送**：一期默认在本地检查待发送答案，可以修改、拒绝或发送；未确认的草稿不上传 Hub。
6. **Alex 收到回复**：在原 Codex 会话中查看答案及来源。

**核心流程：提问 → 对方批准 → 本地 Agent 回答 → 对方确认发送 → 收到回复。**

## 3. 系统由什么组成

```text
Alex 的 Codex ↔ MCP ↔ 云端 Agent Hub
                           ↕ 本地主动建立连接
                     Bob 的本地 Connector
                           ↓
                     本地 Codex / 项目
```

| 组成 | 职责 |
| --- | --- |
| Agent Hub（统一云端服务） | 管理身份、设备、项目映射、执行审批、调度、任务状态和审计；保存离线请求，分发已确认答案 |
| Local Connector（本地轻量执行器） | 主动连接云端、同步注册项目、校验任务授权与本地路径、启动或恢复 Agent、展示草稿供本人确认并回传 |
| Project Agent（项目 Agent） | 在本地指定目录读取上下文并回答；一期优先接入 Codex CLI，预留 Claude Code 等 Runtime 适配入口 |
| MCP 接口 | 让 Codex 能查询联系人与项目、发起请求、查询进度和取消请求 |

**云端知道路径，不等于能直接访问路径。** 真正启动进程的是本地 Connector；不需要向云端开放任意远程命令执行能力。MCP 只是调用入口，不承担平台管理职责。

### 云端如何找到本地项目

注册映射包含：**用户 ID + 项目 ID + 设备 ID + 本地路径 + Runtime**。例如 Bob 的 recommendation 项目，位于他的 MacBook 上的 `~/work/recommendation`，使用 Codex。

- 云端按“个人＋项目”查找设备映射；多设备时由用户指定执行设备，一期不自动跨设备切换。
- 云端按项目 ID 下发已批准任务，本地根据自身注册记录解析目录，而不是直接执行请求方传来的路径或命令。
- 项目迁移或撤销注册后，Connector 更新云端映射；本地记录失效时拒绝执行并提示修正。
- 绝对路径仅用于本人管理和平台内部调度，不向其他提问者展示；对外只展示获准公开的项目名称和描述。

## 4. 如何让 Agent 真正了解项目

访问对象不是笼统的“Bob 的 Agent”，而是“Bob 的 recommendation 项目 Agent”。同一个人可以注册多个项目，底层复用同一个 Codex。

每个项目由三部分组成：

- **项目路径**：云端保存调度映射，本地保留注册记录并在执行前校验，确保请求进入正确项目。
- **稳定项目资料**：在 `AGENTS.md` 中沉淀项目背景、架构、重要决策和关键目录，不完全依赖聊天历史。
- **独立协作会话**：为外部问答维护专门的 Peer Session，保留近期讨论，不混入日常编码会话。

执行时先定位项目，再恢复协作会话；没有可用会话时新建，并读取稳定项目资料。这样会话丢失后仍有基础上下文，但回答质量仍取决于项目资料是否完整。

## 5. 安全与可靠性底线

### 人始终掌握访问和分享权限

- **执行前审批、发送前确认**：一期默认保留两次人工确认，不默认自动执行或发送。
- **只允许访问显式注册的项目**：不能通过提问要求读取用户的任意本地目录。
- **默认只读**：不允许 Agent 自动执行高风险操作。
- **本地强制校验**：检查任务所属用户、设备、项目及审批状态；通过 Runtime 的沙箱和工具权限限制访问，不能仅靠切换工作目录实现隔离。
- **明确数据上云范围**：Hub 保存项目定位信息、问题、审批及任务状态、已确认答案；未确认草稿留在本地，审计日志不夹带代码、密钥或草稿正文。这里不等于本地 Runtime 的模型推理一定离线。
- **保留基础审计**：记录请求、授权、执行及发送过程，便于追溯。

人工审核用于降低错误回答和敏感信息外发的风险，但不能替代实际的权限限制。自动批准、自动发送留待后续按信任关系评估。

### 离线不丢请求，多设备不重复执行

- **先存后发**：Hub 数据库保存请求及状态，电脑离线不影响请求留存，也不强制唤醒电脑。
- **通知与任务分开**：WebSocket 只通知任务变化，Connector 再获取完整任务；重连后同步待处理请求。云端已批准但设备离线的任务继续等待。
- **执行前领取任务**：通过带有效期的任务租约，避免同一请求被多台设备同时执行。
- **进度可查询**：区分等待授权、等待设备、执行中、等待发送确认和已完成等状态。

## 6. 一期做什么、不做什么

| 一期做 | 一期不做 |
| --- | --- |
| 身份识别、联系人与项目请求 | 自动发现专家、自动选择项目 |
| 两阶段人工确认、基础权限和审计 | 企业级复杂权限、默认自动批准或发送 |
| 云端项目映射、本地 Connector、Codex 和协作会话恢复 | 云端 Agent、多 Runtime 全量适配（Claude Code 留作后续适配） |
| 离线请求保存、结果返回、多设备任务领取 | 多 Agent 自主协作、完整 A2A 协议 |
| 基于项目文件和会话复用上下文 | 长期共享 Memory、复杂 RAG / 向量数据库 |

## 7. 怎么落地

采用轻量单体方案：**FastAPI + PostgreSQL + WebSocket + Python Connector + Codex CLI**。云端数据库保存项目映射和任务；本地 JSON / SQLite 保存注册记录与会话映射。MCP 使用 Python SDK，执行审批使用云端 Web 页面，答案确认使用本地界面。

按以下顺序实施：

1. **项目能注册、请求能送达**：完成登录、设备注册、项目路径同步、Connector 连接、云端请求存储和执行审批。
2. **云端能调度、本地能回答**：完成个人与项目定位、设备任务领取、本地授权校验、只读执行和项目资料加载，再加入协作会话恢复。
3. **回复能闭环**：完成答案审核、编辑、发送和请求方接收。
4. **接入自然语言入口**：通过 MCP 让 Alex 直接在 Codex 中发起和跟进请求。

最终 Demo 用两台电脑跑通完整流程，并验证项目注册与路径同步、离线恢复、正确设备与目录定位、未授权任务拒绝执行，以及草稿未经确认不上传 Hub。

## 8. 如何判断值得继续做

一期先看协作价值，不急于看活跃用户规模：

| 观察项 | 要回答的问题 |
| --- | --- |
| 请求成功率 | 发起的请求中，有多少最终完成？ |
| 项目上下文命中情况 | Agent 是否进入正确项目并使用相关资料？ |
| 回答可直接发送的比例、人工修改比例 | Agent 是否真正减少了 Bob 查资料和整理答案的工作？ |
| 信息同步耗时 | 相比直接找人询问，是否更快？比较时包含两次审批的等待时间 |
| 持续使用意愿 | Demo 之后，团队成员是否愿意继续使用？ |

重点关注三个风险：**资料不足导致答不好、敏感信息外发、两次审批负担过重**。一期先用稳定项目资料、访问限制和人工审核降低风险，再根据实际使用决定是否增加可信联系人自动化、专家发现或云端能力。

---

**一期要证明的不是“两个模型可以聊天”，而是“一个人的 Agent 能否在本人授权和项目权限边界内，可靠地替他完成信息同步”。**
